extends layout

block content
  h1= title
  p Welcome to #{title}

  form(action="/snapshot", method="GET")
    input(type="text", name="wkn")
    button Get stock data


  form(action="/history", method="GET")
    input(type="text", name="wkn")
    button Get stock historical data